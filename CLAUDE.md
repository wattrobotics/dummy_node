# dummy_node 작업 규약

이 저장소에서 작업할 때 지켜야 할 사항이다. 패키지 구조와 인터페이스 사용법은
[README.md](README.md) 에 있으므로 여기서는 반복하지 않는다.

## 브랜치와 PR

`main` 은 PR 을 거치도록 규칙이 걸려 있으나, **저장소 admin(소유자)은 우회할 수 있다.**
따라서 작업 주체에 따라 다르게 한다.

- **소유자 본인과 그 자격으로 작업하는 도구(Claude 포함)**: `main` 에 바로 커밋하고
  푸시한다. 매번 PR 을 왕복하면 반복 속도가 너무 떨어지기 때문이다.
  푸시하면 CI 가 사후에 돌므로, 깨졌다면 그때 고친다.
- **외부 기여자**: 브랜치와 PR 을 거치며 CI 통과가 필수다.

```bash
# 소유자 작업 (기본)
git add -A && git commit -m "feat: ..." && git push

# 외부 기여 또는 리뷰가 필요한 큰 변경
git checkout -b feat/무엇을-한다
git push -u origin feat/무엇을-한다
gh pr create --fill
```

리뷰가 필요할 만큼 크거나 되돌리기 어려운 변경이라면 소유자라도 PR 을 쓴다.
판단이 서지 않으면 물어본다.

## 커밋 메시지

한국어로 쓰고 `<타입>: <요약>` 형태를 지킨다. 요약만으로 부족한 변경은 본문에
**왜 그렇게 했는지**를 적는다 — 무엇을 바꿨는지는 diff 가 이미 말해 준다.

기존 이력이 이 형태이므로 그대로 따른다.

## 코드 관례

- **주석과 문서는 한국어로 쓴다.** 기존 파일 전체가 그렇다.
- **문자열은 큰따옴표를 쓴다.** flake8 의 `Q000` 경고가 나오지만 이 저장소의 관례이며,
  기존 파일에도 동일하게 나온다. 린터에 맞추려고 기존 코드를 바꾸지 않는다.
- import 순서도 기존 파일과 같은 방식을 따른다(표준 → `rclpy` → 메시지 패키지 →
  `dummy_node`). flake8 의 `I100` 경고 역시 기존 파일에 동일하게 존재한다.

## 반드시 지킬 설계 규칙

세 가지는 어기면 **조용히 고장난다** — 노드가 죽지 않고 해당 기능만 빠지거나
아무것도 수신되지 않으므로, 원인을 찾기 어렵다.

1. **네임스페이스**: 인터페이스는 항상 상대 이름만 쓴다(`"my_topic"`).
   코어 노드가 `namespace="dummy"` 로 생성되므로 자동으로 `/dummy/...` 가 된다.
   `~/` 나 절대경로는 규칙을 깬다.

2. **공유 파라미터**: 둘 이상의 인터페이스가 쓰는 파라미터는 반드시
   `InterfaceBase.param()` 을 쓴다. `declare_parameter` 를 두 번 호출하면
   예외가 나고, 로드가 개별 `try/except` 로 감싸여 있어 **그 인터페이스만 조용히
   빠진다.** `tray_count` 가 그 예다.

3. **QoS 일치**: 구독자의 QoS 는 발행자와 정확히 같아야 한다. 어긋나면 오류 없이
   아무것도 수신되지 않는다. 특히 `tray_door/state` 만 `transient_local` 이고
   나머지 상태 토픽은 `volatile` 이다.

`dummy_node/dummy_node/web/` 에는 규칙이 하나 더 있다.

4. **`DummyWebNode` 의 비공개 속성에는 `_web_` 접두사를 붙인다.** `rclpy.Node` 가
   `_clients` · `_services` · `_publishers` 를 내부 장부로 쓰기 때문이며, 덮어쓰면
   그 자리가 아니라 한참 뒤의 호출에서 엉뚱한 예외로 터진다.
   `_assert_no_attr_collision()` 이 기동 시 이 규칙을 강제한다.

## 변경 후 확인

CI 가 빌드와 기동을 검증하지만, 동작을 바꿨다면 올리기 전에 직접 확인한다.

```bash
cd ~/ros2_ws
colcon build --packages-select dummy_node_interfaces dummy_node
source install/setup.bash
ros2 launch dummy_node dummy_node.launch.py
# 웹 콘솔: http://localhost:8080
```

웹 콘솔과 터미널 명령을 **번갈아** 조작해도 화면이 어긋나지 않아야 한다.
화면이 자체 상태를 보관하지 않고 수신한 토픽만 그리기 때문이며, 이 성질이 깨지면
설계가 틀어진 것이다.

## 문서 갱신

인터페이스나 파라미터를 추가하면 `README.md` 의 해당 표에도 반영한다.
지원/미지원 목록은 **사실대로** 유지한다 — 실제로 되지 않는 것을 지원한다고
적지 않는다.
